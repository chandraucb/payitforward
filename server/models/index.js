const User = require('./User');
const Post = require('./Post');
const Organization = require('./Organization');
const Project = require('./Project');
const Profile = require('./Profile');

module.exports = { User, Post, Organization, Project, Profile };
