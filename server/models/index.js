const User = require('./User');
const Post = require('./Post');
const Organization = require('./Organization');
const Project = require('./Project');

module.exports = { User, Post, Organization, Project };
